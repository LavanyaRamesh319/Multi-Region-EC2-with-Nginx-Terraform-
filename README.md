# Multi-Region-EC2-with-Nginx-Terraform

This project uses **Terraform** to create two EC2 instances in different AWS regions and automatically install **Nginx**.

## Prerequisites
- AWS account with access keys configured (`aws configure`)
- Terraform installed

## Usage
```bash
terraform init
terraform apply
